{
  "name": "pizza",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch:ts": "tsc --watch",
    "watch:sass": "sass --watch assets/styles/scss/style.scss assets/styles/css/style.css",
    "watch": "concurrently \"npm run watch:ts\" \"npm run watch:sass\""
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "sass": "^1.79.4"
  },
  "devDependencies": {
    "concurrently": "^8.0.1",
    "typescript": "^5.6.2"
  }
}
